<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <link href="https://fonts.googleapis.com/css?family=Courgette" rel="stylesheet">

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="top-bar">
      <h5 class="header">TravelAPP</h5>
        <% if current_user %>
          <h5><%= current_user.user_name %></h5>
          <h5><%= link_to "Sign Out", destroy_user_session_path, method: :delete%></h5>
        <%else%>
          <h5><%= link_to "Sign In", user_session_path %></h5>
          <h5><%= link_to "Sign Up", new_user_registration_path %></h5>
        <%end %>
    </div>

    <% if flash[:notice] %>
      <div class="notice"><%= flash[:notice] %></div>
    <% end %>

    <%= yield %>
  </body>
</html>
